apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: same-namespace
spec:
  selector:
    app: server
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
